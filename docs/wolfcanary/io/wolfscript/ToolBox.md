## ToolBox __class__

>io.wolfscript.ToolBox

---

### Class Overview

Set of miscellaneous tools

Method | Type   
--- | :--- 
static readonly property __UnixTimestamp__ <br> _Get: Get the Unix timestamp for the current time_ | `long`
static function __formatTimestamp__(timestamp) <br> _Formats a Unix timestamp into the date format defined in server.cfg_ | `String`
static function __arrayMerge__() <br> _Check if an array contains a specified value_ | `int[]`
static function __formatTimestamp__(timestamp, format) <br> _Form ats a Unix timestamp into the date format specified by `format`_ | `String`
static function __floorToBlock__(num) <br> _Round a entity ordinate to a valid block location component_ | `int`
static function __getTimeUntil__(time, delay) <br> _Gets a readable string for the days/hours/minutes/seconds until a period of time_ | `String`
static function __parseWorld__(world, autoLoad) <br> _Parses a fully qualified world name as generated by World.getFqName() to a [`World`](api/world/World.md) object._ | [`World`](api/world/World.md)
static function __getOfflineUUID__(username) <br> _getOfflineUUID method_ | `UUID`
static function __parseTime__(time, timeUnit) <br> _Parse number of seconds for the given time and TimeUnit String_ | `long`
static function __parseTimeInMillis__(time, timeUnit) <br> _Parse number of milliseconds for the given time and TimeUnit String_ | `long`
static function __parseTime__(time, unit) <br> _Parse number of seconds for the given time and TimeUnit_ | `long`
static function __isValidUsername__(username) <br> _Checks if a given user name is a valid Minecraft Username_ | `boolean`
static function __parseWorld__(world) <br> _Parses a fully qualified world name as generated by World.getFqName() to a [`World`](api/world/World.md) object._ | [`World`](api/world/World.md)
static function __isUUID__(uuid) <br> _Checks if a string is a UUID_ | `boolean`
static function __getTimeUntil__(time) <br> _Gets a readable string for the days/hours/minutes/seconds until a period of time_ | `String`
static function __levelToExperience__(level) <br> _Converts World relative time into a 24 hour clock format_ | `int`
static function __parseWorldName__(world) <br> _Takes <code>World</code> and turns it into <code>World_Normal</code>_ | `String`
static function __parseWorldName__(world, dimension) <br> _Gets the FQ world name for the base world and [`DimensionType`](api/world/DimensionType.md)_ | `String`
static function __stringToNull__(str) <br> _Merge 2 arrays. This will remove duplicates._ | `String`
static function __usernameToUUID__(username) <br> _Ask's Mojang's API for a UUID for a give UserName_ | `String`
static function __uuidFromUsername__(username) <br> _Asks Mojang's API for a UUID for a given UserName_ | `UUID`
static function __worldTicksToCalendar__(ticks) <br> _Generates a Calendar(GMT, English) object representing the current time of day from the time of a world._ | `Calendar`



---


### Public Properties for [`ToolBox`](ToolBox.md)

##### <a id='unixtimestamp'></a>public static readonly property __UnixTimestamp__

_Get: Get the Unix timestamp for the current time_

Get | Description
--- | --- 
`long` | `long` timestamp



---

### Public Methods for [`ToolBox`](ToolBox.md)

##### <a id='formattimestamp'></a>public static function __formatTimestamp__(timestamp)

_Formats a Unix timestamp into the date format defined in server.cfg_

Argument | Type | Description  
--- | --- | --- 
timestamp | `long` | the `long` time

Returns | Description
--- | --- 
`String` | `String` formatted TimeStamp


##### <a id='arraymerge'></a>public static function __arrayMerge__()

_Check if an array contains a specified value_

Returns | Description
--- | --- 
`int[]` | true if needle is found in haystack, false otherwise /
    public static <T> boolean arrayContains(T[] haystack, T needle) {
        // return Arrays.asList(haystack).contains(needle); // could be this but in some cases pure iterating is slightly faster
        for (T type : haystack) {
            if (type != null && type.equals(needle)) {
                return true;
            }
        }
        return false;
    }

    /** Merge 2 arrays. This will just merge two arrays.


##### <a id='formattimestamp'></a>public static function __formatTimestamp__(timestamp, format)

_Form ats a Unix timestamp into the date format specified by `format`_

Argument | Type | Description  
--- | --- | --- 
timestamp | `long` | The time to get formatted
format | `String` | The pattern describing the date and time format

Returns | Description
--- | --- 
`String` | `String` formatted TimeStamp


##### <a id='floortoblock'></a>public static function __floorToBlock__(num)

_Round a entity ordinate to a valid block location component This takes into account the rounding issues in negative x/z direction_

Argument | Type | Description  
--- | --- | --- 
num | `double` | the `double` value to round to the nearest lower integer

Returns | Description
--- | --- 
`int` | the rounded number


##### <a id='gettimeuntil'></a>public static function __getTimeUntil__(time, delay)

_Gets a readable string for the days/hours/minutes/seconds until a period of time_

Argument | Type | Description  
--- | --- | --- 
time | `long` | the Unix-TimeStamp to start from
delay | `long` | the delay from the start point until expiration

Returns | Description
--- | --- 
`String` | the String representation of the time until


##### <a id='parseworld'></a>public static function __parseWorld__(world, autoLoad)

_Parses a fully qualified world name as generated by World.getFqName() to a [`World`](api/world/World.md) object. Also parses fully qualified world names with <code>WorldName:</code>[`DimensionType`](api/world/DimensionType.md) If only the world group name is given (that's "default" by default), then the NORMAL dimension will be assumed. <p/> Will auto load the world if `autoLoad` is `true`_

Argument | Type | Description  
--- | --- | --- 
world | `String` | the fully qualified world name or a world group name.
autoLoad | `boolean` | Will auto load the world if `true`

Returns | Description
--- | --- 
[`World`](api/world/World.md) | the world that was parsed or null if the world didn't exist or wasn't loaded.


##### <a id='getofflineuuid'></a>public static function __getOfflineUUID__(username)

_getOfflineUUID method_

Argument | Type | Description  
--- | --- | --- 
username | `String` | username argument

Returns | 
--- | 
`UUID` |


##### <a id='parsetime'></a>public static function __parseTime__(time, timeUnit)

_Parse number of seconds for the given time and TimeUnit String<br> Example: long 1 String HOUR will give you number of seconds in 1 hour.<br> This is used to work with Unix timestamps._

Argument | Type | Description  
--- | --- | --- 
time | `long` | the `long` time
timeUnit | `String` | MINUTES, HOURS, DAYS, WEEKS, MONTHS

Returns | Description
--- | --- 
`long` | `long` parsed time


##### <a id='parsetimeinmillis'></a>public static function __parseTimeInMillis__(time, timeUnit)

_Parse number of milliseconds for the given time and TimeUnit String<br> Example: long 1 String HOUR will give you number of seconds in 1 hour.<br> This is used to work with Unix timestamps._

Argument | Type | Description  
--- | --- | --- 
time | `long` | the `long` time
timeUnit | `String` | MINUTES, HOURS, DAYS, WEEKS, MONTHS

Returns | Description
--- | --- 
`long` | `long` parsed time


##### <a id='parsetime'></a>public static function __parseTime__(time, unit)

_Parse number of seconds for the given time and TimeUnit<br> Example: long 1 String `TimeUnit#HOURS` will give you number of seconds in 1 hour.<br> This is used to work with unix timestamps._

Argument | Type | Description  
--- | --- | --- 
time | `long` | the `long` time
unit | `TimeUnit` | the `TimeUnit` to use for conversion

Returns | Description
--- | --- 
`long` | `long` parsed time


##### <a id='isvalidusername'></a>public static function __isValidUsername__(username)

_Checks if a given user name is a valid Minecraft Username_

Argument | Type | Description  
--- | --- | --- 
username | `String` | the username to check

Returns | Description
--- | --- 
`boolean` | `true` of valid; `false` if not


##### <a id='parseworld'></a>public static function __parseWorld__(world)

_Parses a fully qualified world name as generated by World.getFqName() to a [`World`](api/world/World.md) object. Also parses fully qualified world names with <code>WorldName:</code>[`DimensionType`](api/world/DimensionType.md) If only the world group name is given (that's "default" by default), then the NORMAL dimension will be assumed. <p/> Will not auto load the world if not loaded_

Argument | Type | Description  
--- | --- | --- 
world | `String` | the fully qualified world name or a world group name.

Returns | Description
--- | --- 
[`World`](api/world/World.md) | the world that was parsed or null if the world didn't exist or wasn't loaded.


##### <a id='isuuid'></a>public static function __isUUID__(uuid)

_Checks if a string is a UUID_

Argument | Type | Description  
--- | --- | --- 
uuid | `String` | the string uuid

Returns | Description
--- | --- 
`boolean` | `true` if UUID; `false` if not


##### <a id='gettimeuntil'></a>public static function __getTimeUntil__(time)

_Gets a readable string for the days/hours/minutes/seconds until a period of time_

Argument | Type | Description  
--- | --- | --- 
time | `long` | the Unix-TimeStamp of (or amount of seconds until) the future time expiration

Returns | Description
--- | --- 
`String` | the String representation of the time until


##### <a id='leveltoexperience'></a>public static function __levelToExperience__(level)

_Converts World relative time into a 24 hour clock format_

Argument | Type | Description  
--- | --- | --- 
level | `int` | the level you want to get the Experience points for

Returns | Description
--- | --- 
`int` | 24 hour clock formatted string of the time /
    public static String worldTimeTo24hClock(long ticks) {
        SimpleDateFormat dateFormat = new SimpleDateFormat("HH:mm");
        dateFormat.setTimeZone(tz_GMT);
        return dateFormat.format(worldTicksToCalendar(ticks).getTime());
    }

    /** Converts World relative time into a 12 hour clock format


##### <a id='parseworldname'></a>public static function __parseWorldName__(world)

_Takes <code>World</code> and turns it into <code>World_Normal</code> Takes <code>World:NORMAL</code> and turns it into <code>World_NORMAL</code>_

Argument | Type | Description  
--- | --- | --- 
world | `String` | The FQ name, main world name, or the FQ name with <code>:</code> and not <code>_</code>

Returns | Description
--- | --- 
`String` | The right FQ name


##### <a id='parseworldname'></a>public static function __parseWorldName__(world, dimension)

_Gets the FQ world name for the base world and [`DimensionType`](api/world/DimensionType.md) Takes <code>World</code> and turns it into <code>World_Normal</code>_

Argument | Type | Description  
--- | --- | --- 
world | `String` | main world's name
dimension | [`DimensionType`](api/world/DimensionType.md) | The dimension, can be `null`

Returns | Description
--- | --- 
`String` | The right FQ name


##### <a id='stringtonull'></a>public static function __stringToNull__(str)

_Merge 2 arrays. This will remove duplicates._

Argument | Type | Description  
--- | --- | --- 
str | `String` | the `String` to parse

Returns | Description
--- | --- 
`String` | array containing all elements of the given 2 arrays, minus duplicate entries /
    public static <T> T[] safeArrayMerge(T[] first, T[] second, T[] template) {
        HashSet<T> res = new HashSet<T>();

        Collections.addAll(res, first);
        Collections.addAll(res, second);
        return res.toArray(template);
    }

    /** If the given string is "null" or null, this method returns null, otherwise it will return the string as it was passed


##### <a id='usernametouuid'></a>public static function __usernameToUUID__(username)

_Ask's Mojang's API for a UUID for a give UserName_

Argument | Type | Description  
--- | --- | --- 
username | `String` | the user name to get a UUID for

Returns | Description
--- | --- 
`String` | user's uuid or null if not found/on error


##### <a id='uuidfromusername'></a>public static function __uuidFromUsername__(username)

_Asks Mojang's API for a UUID for a given UserName_

Argument | Type | Description  
--- | --- | --- 
username | `String` | the user name to get a UUID for

Returns | Description
--- | --- 
`UUID` | user's uuid or null if not found/on error


##### <a id='worldtickstocalendar'></a>public static function __worldTicksToCalendar__(ticks)

_Generates a Calendar(GMT, English) object representing the current time of day from the time of a world. This time can be retrieved from World.getRelativeTime(). Year, month and day values may be odd as those are not contained within the range of the world tick times in Minecraft._

Argument | Type | Description  
--- | --- | --- 
ticks | `long` | the relative time of a world

Returns | Description
--- | --- 
`Calendar` | Calendar object representing the world time as real date


---


###### This file was system generated using custom scripts copyright (c) 2015 Mining Wolf.
	

